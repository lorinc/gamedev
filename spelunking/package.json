{
  "name": "spelunking",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000 --bind 127.0.0.1",
    "check": "tsc",
    "test": "node --test",
    "gallery": "node tools/gallery.js",
    "deploy": "tools/deploy.sh"
  },
  "devDependencies": {
    "typescript": "~5.9.0"
  }
}
